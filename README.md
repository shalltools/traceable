# traceable
Annotate Rust tests with formal requirement IDs and generate traceability matrices. Includes the #[req(...)] attribute (traceable crate) and a CLI for validation and matrix generation (traceable-cli).
